# Whisperrr Python Service Configuration (OPTIONAL)
#
# NOTE: All defaults are defined in python-service/app/config.py
# This file is optional - only use if you need to override defaults.
# The config.py file is the single source of truth for all configuration.
#
# To use: Copy this file to .env (but .env is gitignored, so it won't be committed)
# Or set environment variables directly in your deployment environment.

# Model Configuration
# Override model size (default: base, defined in config.py)
# MODEL_SIZE=base

# Override max file size in MB (default: 1000MB/1GB, defined in config.py)
# MAX_FILE_SIZE_MB=1000

# Override concurrent transcriptions (default: 3, defined in config.py)
# MAX_CONCURRENT_TRANSCRIPTIONS=3

# File System
# Override upload directory (default: /tmp/whisperrr_uploads, defined in config.py)
# UPLOAD_DIR=/tmp/whisperrr_uploads

# Override cleanup setting (default: true, defined in config.py)
# CLEANUP_TEMP_FILES=true

# Logging
# Override log level (default: INFO, defined in config.py)
# LOG_LEVEL=INFO

# CORS Configuration
# Override CORS origins (default: http://localhost:8080,http://localhost:3000, defined in config.py)
# CORS_ORIGINS=http://localhost:8080,http://localhost:3000

# API Configuration (these are rarely overridden)
# API_TITLE=Whisperrr Transcription Service
# API_DESCRIPTION=High-quality audio transcription using OpenAI Whisper
# API_VERSION=1.0.0

# Performance
# ENABLE_METRICS=true
# ENABLE_HEALTH_CHECKS=true
